<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Accès_Cours - Login</title>

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>

{% block body %}
    <div id="auth">

        <div class="row h-100">
            <div class="col-12">
                <div class="container">
                    <div class="auth-logo mb-5 mt-10">
                        <a href="index.html"><img src="{{ asset("/images/logo/logo.png") }}" alt="Logo"></a>
                    </div>
                    <h1 class="auth-title mb-10">Connexion</h1>
                    <form method="post">
                        {% if error %}
                            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                        {% endif %}

                        <div class="form-group position-relative has-icon-left mb-4">
                            <input name="email" value="{{ last_username }}" type="text" class="form-control form-control-xl" placeholder="Nom d'utilisateur" autocomplete="email" required autofocus>
                            <div class="form-control-icon">
                                <i class="bi bi-person"></i>
                            </div>
                        </div>
                        <div class="form-group position-relative has-icon-left mb-4">
                            <input name="password" type="password" class="form-control form-control-xl" placeholder="Mot de passe" autocomplete="current-password" required>
                            <div class="form-control-icon">
                                <i class="bi bi-shield-lock"></i>
                            </div>
                        </div>
{#                        <div class="form-check form-check-lg d-flex align-items-end">#}
{#                            <input class="form-check-input me-2" type="checkbox" value="" id="flexCheckDefault">#}
{#                            <label class="form-check-label text-gray-600" for="flexCheckDefault">#}
{#                                Se souvenir de moi#}
{#                            </label>#}
{#                        </div>#}
                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                        <button class="btn btn-primary btn-block btn-lg shadow-lg mt-5">Connexion</button>
                    </form>
                    <div class="text-center mt-5 text-lg fs-4">
                        <p class="text-gray-600">Si vous n'avez pas de compte <a href="{{ path('etudiant_register') }}" class="font-bold">Inscrivez-vous</a>.</p>
                        <p><a class="font-bold" href="auth-forgot-password.html">Mot de passe oublier?</a>.</p>
                    </div>
                </div>
            </div>
        </div>

    </div>
{% endblock %}

</body>
</html>

