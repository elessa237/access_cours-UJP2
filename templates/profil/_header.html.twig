<div class="bg-white rounded-bottom smooth-shadow-sm ">
    <div class="d-flex align-items-center justify-content-between
                  pt-4 pb-6 px-4">
        <div class="d-flex align-items-center">
            <!-- avatar -->
            <div class="avatar-xxl avatar-indicators avatar-online me-2
                      position-relative d-flex justify-content-end
                      align-items-end mt-n10">
                <img src="{{ asset("images/avatar/avatar-1.jpg") }}" class="avatar-xxl
                        rounded-circle border border-4 border-white-color-40" alt="">
                <div class="position-absolute top-0 right-0 me-2" data-bs-toggle="tooltip"
                     data-placement="top" title="" data-original-title="Verified">
                    <img src="{{ asset("images/svg/checked-mark.svg") }}" alt="" height="30" width="30">
                </div>
            </div>
            <!-- text -->
            <div class="lh-1">
                <h2 class="mb-0">{{ app.user.username }}</h2>
                <p class="mb-0 d-block">{{ app.user.email }}</p>
            </div>
        </div>
        <div>
            <a href="#" class="btn btn-outline-primary
                      d-none d-md-block">Edit Profile</a>
        </div>
    </div>
</div>
