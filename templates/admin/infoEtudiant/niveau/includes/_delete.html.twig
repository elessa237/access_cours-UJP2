<form method="post" action="{{ path('delete_niveau', {id : niveau.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer ce niveau?')">
    <input type="hidden" name="_token" value="{{ csrf_token('delete'~niveau.id) }}">
    <button class="btn"><i class="fa fa-trash-o m-r-5"></i></button>
</form>